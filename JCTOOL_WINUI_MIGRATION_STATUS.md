# jctool WinUI 3 迁移进展

更新时间：2026-09-04

## 当前结论

WinUI 3 新前端已建立并可编译运行。旧 WinForms/HID 主程序仍保留，尚未切换为新版默认入口。当前新版定位为 x86、非打包、自包含 WinUI 3 应用，最低目标 Windows 10 1809（`10.0.17763.0`）。

## 已完成

- 固定深色 WinUI 3 外壳：顶部控制器栏、导航栏、内容区、任务状态栏。
- 响应式布局：窗口变窄时导航收窄，页面由双栏切换为单栏，内容区可滚动。
- DPI：使用 Per-Monitor V2 清单；已在当前高 DPI 环境下检查标准和最小窗口布局。
- 窗口状态：持久化位置、大小、最大化状态；恢复时限制在当前显示器工作区内。
- 设备发现：最多四个控制器，按设备路径稳定分配槽位，支持模拟器和物理 HID 枚举。
- 设备详情：固件、MAC、电池百分比/电压、温度和 SPI 颜色读取。
- 颜色与 SPI：读取颜色、受限 SPI 读取、颜色写入确认、写后回读校验、控制器识别震动。
- 校准：读取 `0x8010`、`0x8026`、`0x6089`、`0x609B`；支持用户校准和摇杆设备参数写入，包含身份绑定、数值顺序检查和回读校验。
- HD 震动：支持 `.jcvib`/`RRAW`/`.bnvib` 文件校验、加载、后台播放和停止。
- 输入测试：支持一次性读取和连续输入流；显示按键、摇杆、加速度计、陀螺仪、连接方式、电池和充电状态，并按 Joy-Con (L)、Joy-Con (R)、Pro Controller 映射 22 个按键的可用与按下状态。
- 连续输入流：原生桥接保持单次 HID 会话，界面支持启动/停止；离开页面、切换设备和关闭窗口时会取消并恢复设备输入模式。
- IR 摄像头：支持 Joy-Con (R) 拍照、PNG 保存和视频流；支持 40×30、80×60、160×120、320×240，曝光、增益、补光、外部光线滤波、降噪和水平翻转，以及灰度、夜视和红外显示处理。
- NFC：支持 Joy-Con (R) 和 Pro Controller 标签扫描、UID/类型识别、NTAG213/215/216 原始页读取，以及 NDEF 文本和 URI 记录解析。
- 后台任务：SPI、震动、输入流、IR 和 NFC 共用任务占用、状态、确定/不确定进度、取消和错误清理；任务状态跨页面保留。
- SPI 备份与恢复：支持完整 `512 KiB` 分块备份、逐块二次读取校验、SHA-256 伴随文件、产品/蓝牙身份匹配，以及颜色、序列号、用户校准和完整安全范围恢复；所有写入仍受原生安全区限制。
- 序列号：Joy-Con 支持可打印 ASCII 序列号写入、备份恢复和写后回读校验；Pro Controller 明确禁用。
- HD 震动转换：支持 `.bnvib` 普通、循环、循环/等待头型；沿用旧版幅度 LUT、低/高频幅度与频率均衡、安全幅度归一化，并在播放时展开循环/等待。
- Debug/Custom/Internal：提供诊断页和原生硬件安全白名单；支持安全 Debug 查询、受限 Internal 命令、回复转储，以及完整恢复收尾的出货/清配对/重启配对动作。
- 硬件预检：诊断页可直接点击运行，命令行仍支持 `--hardware-preflight`；两者共用只读/启停验收入口，覆盖输入、SPI 只读、白名单拒绝、IR 启停和 NFC 取消，不会写 SPI 或修改配对。
- 术语与无障碍：新增页面控件均使用 `en-US`/`zh-CN` 资源；图标按钮、IR 预览、控制器槽位和动态按键状态均有 UI Automation 名称或工具提示。
- 布局矩阵：当前 Windows 环境已在 760、900、1200 宽度下检查全部 9 个页面，未发现启动失败、控件重叠或不可见溢出。
- 多语言：已建立 `en-US` 和 `zh-CN` 两套 `.resw` 资源，共 315 个对应键；已实现系统语言、简体中文、English 选择及重启生效流程。

## 尚未迁移

- 旧版独立颜色选择器的完整替换。
- IR 自动曝光、运行中实时参数调整、Ironbow 调色和手势分析模式。
- MIFARE 标签内容读取；当前只识别 UID 和标签类型，与旧版限制一致。
- 真实控制器验收：当前仅完成模拟器和只读预检，尚未在实体 Joy-Con/Pro Controller 上执行全部写入、IR/NFC 时序和重启配对流程。

## 验证记录

- Release 编译通过：
  `MSBuild jctool.vs2017-net4.7.1.sln /t:JcTool_WinUI /p:Configuration=Release /p:Platform=x86`
- 综合模拟器自检通过，退出码 `0`：
  `JcTool.WinUI.exe --operations-self-test`
- 自检覆盖：四设备发现、设备详情读取、SPI 读取、颜色同值写回校验、校准区域同值写回校验、识别震动、原始震动、输入快照、连续读取三帧、输入取消、IR 四档分辨率成帧、IR 首分片取消及重开、NFC UID/NTAG213/NDEF 解析、NFC 取消、完整 `512 KiB` SPI 备份及逐块校验、颜色恢复、备份身份/产品篡改拒绝、四种震动文件头和循环展开、安全 Debug/内部命令白名单和拒绝。
- 已生成并检查简体中文的概览、颜色/SPI（含备份面板）、校准、HD 震动（含均衡器）、输入测试、IR、NFC、诊断和设置页面截图；标准及窄窗口未发现控件重叠。
- 英文和简体中文资源键数量一致：`315 / 315`；代码引用的资源键无缺失。

## 已知限制

- 当前只验证了模拟器，尚未在真实 Joy-Con/Pro Controller 上验证所有读写操作。
- IR/NFC 的真实设备 MCU 初始化、分片重传和退出恢复尤其需要单独验证；模拟器通过不代表硬件时序已经确认。
- 新版前端仍与旧版业务代码并行，未完成最终发布目录、安装/更新方案和默认入口切换。
- 当前构建目标为 x86；若要支持 x64/ARM64，需要分别构建原生桥接并验证 HID 行为。
- 语言设置页已实现资源切换和重启请求；应用重启后的完整交互流程仍需人工确认。

## 下一步建议

1. 由用户运行 `--hardware-preflight`，再在实体设备上验收写入、IR/NFC 时序、退出恢复和重启配对流程。
2. 在 Win10/Win11 的 100%、125%、150%、175%、200% DPI，以及多显示器拖动场景下做发布前验收。
3. 可选：完成旧版独立颜色选择器的完整替换，以及 IR 自动曝光/实时调参/更多图像处理模式。
4. 用户确认 WinUI 3 验收通过后，再决定是否切换默认入口，并保留旧 WinForms 作为回退版本。

## 主要文件

- `jc_toolkit/jctool-winui/`：WinUI 3 前端、MVVM、页面和多语言资源。
- `jc_toolkit/native/`：原生 HID 桥接 DLL。
- `jc_toolkit/jctool.vs2017-net4.7.1.sln`：已加入 `JcTool.WinUI` 和 `JcTool.Native` 项目。
- `jc_toolkit/jctool/`：原有 WinForms/HID 实现，当前作为行为基准保留。
